import React from "react";

/**
 * PUBLIC_INTERFACE
 * Displays user’s favorite recipes.
 * Allows users to view, unfavorite, and quickly access saved recipes.
 * Viewable on the User Profile or as a standalone section.
 */
function Favorites() {
  return (
    <section>
      {/* Favorite recipes list */}
    </section>
  );
}

export default Favorites;
