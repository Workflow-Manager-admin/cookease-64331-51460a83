import React from "react";

/**
 * PUBLIC_INTERFACE
 * Shows and allows editing of user profile.
 * Displays user's favorites, recipe history, and settings.
 * Only accessible if user is authenticated.
 */
function UserProfile() {
  return (
    <section>
      {/* User data, favorites, history */}
    </section>
  );
}

export default UserProfile;
