import React from "react";

/**
 * PUBLIC_INTERFACE
 * The Navbar sits at the top of the app.
 * Contains logo, search bar (on mobile), theme toggle, profile access, auth actions, etc.
 */
function Navbar() {
  return (
    <nav>
      {/* Logo, navigation, theme toggle, profile/auth buttons */}
    </nav>
  );
}

export default Navbar;
