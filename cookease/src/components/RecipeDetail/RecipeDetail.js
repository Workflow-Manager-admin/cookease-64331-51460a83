import React from "react";

/**
 * PUBLIC_INTERFACE
 * Shows detailed view for a single recipe:
 * - Title, images, ingredient list, step-by-step instructions, nutrition info.
 * - "Add to shopping list", save favorite, etc.
 */
function RecipeDetail({ recipeId }) {
  return (
    <section>
      {/* Show recipe detail by id */}
    </section>
  );
}

export default RecipeDetail;
