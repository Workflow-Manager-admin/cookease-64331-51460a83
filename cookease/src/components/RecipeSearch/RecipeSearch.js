import React from 'react';

/**
 * PUBLIC_INTERFACE
 * RecipeSearch
 * Responsible for taking user input (ingredient, dish name) and initiating a search for recipes.
 */
const RecipeSearch = () => {
  return (
    <section className="recipe-search">
      {/* Search bar and form for recipe queries */}
      Recipe Search Placeholder
    </section>
  );
};

export default RecipeSearch;
