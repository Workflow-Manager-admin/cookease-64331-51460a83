import React from 'react';

/**
 * PUBLIC_INTERFACE
 * LoadingIndicator
 * Shows a spinner or animation during pending API/data loads.
 */
const LoadingIndicator = () => {
  return (
    <div className="loading-indicator">
      {/* Loading animation or message */}
      Loading...
    </div>
  );
};

export default LoadingIndicator;
